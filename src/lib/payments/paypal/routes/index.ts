/** 
 * https://github.com/atmulyana/nextCart
 **/
import type {GatewayRoutes} from '../../types';

export default {
    checkout_action: 'route',
    checkout_cancel: 'route',
    checkout_return: 'route',
} as GatewayRoutes;